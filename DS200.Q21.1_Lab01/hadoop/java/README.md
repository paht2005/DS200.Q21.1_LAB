# Java MapReduce (Lab 01)

Full documentation, task flows, and the **Java vs Python** roles live in the lab root:

**[../../README.md](../../README.md)**

## Quick build

```bash
cd lab01-mapreduce
mvn -q package -DskipTests
```

Artifact: `lab01-mapreduce/target/lab01-mapreduce-1.0.0.jar` (add `target/` is gitignored — only source is versioned).

## Run all tasks (local Hadoop)

From `DS200.Q21.1_Lab01`:

```bash
bash scripts/run_java_mapreduce_local.sh
```
